# AGENTS.md

このリポジトリで作業する AI エージェント向けのルールです。

## 基本方針

- 既存の設計・命名・ディレクトリ構成に合わせて、最小限の変更で対応してください。
- 要求されていない機能追加や大規模なリファクタリングは行わないでください。
- 判断に迷う場合は、勝手に進めず、前提と選択肢を明示してください。

## パッケージ管理

- パッケージマネージャーは `npm` ではなく `yarn` を使用してください。
- `npm install`、`npm ci`、`npx` は使用しないでください。
- 必要な場合は `yarn add`、`yarn install`、`yarn dlx` を使用してください。
- node と yarn は `~/.volta/bin/` にインストールされています。

## 実装

- 何かを実装した後には、必ず `yarn build` / `yarn test` / `yarn lint` を実行して、コードが正しく動作し、スタイルガイドに準拠していることを確認してください。
